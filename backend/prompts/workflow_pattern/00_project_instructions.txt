OBJECTIVE
Confirm the scope and context for the Workflow Pattern before any pattern content is generated.

INPUT CONTEXT

Topic: {{topic}}
Jurisdiction: {{jurisdiction}}
Component: {{component}}
Context: {{context}}

INSTRUCTIONS

Before generating any pattern content, confirm the following with the user:

• Pattern name (if known)
• Operational problem the workflow solves
• Where in the lifecycle it applies (Instruction, Matter, child Work Type, governance overlay, etc.)
• Trigger type (if known)
• Whether the pattern must be cross-jurisdictional

If any of these are unclear, request clarification before proceeding.

Do NOT generate pattern content at this step.

OUTPUT FORMAT

Confirmation questions only.

Validation Checklist

• Scope clarity confirmed
• Lifecycle positioning clear
• No pattern content generated